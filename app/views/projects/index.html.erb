    <% @projects.each do |project|%>

        <%= link_to project.title, user_project_path(project.user, project) %><br> 
                
        <%= project.description%> <br>

        <%= link_to project.hobby.title, project.hobby%><br> <br>

    <%end%>
